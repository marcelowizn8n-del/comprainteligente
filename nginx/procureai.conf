server {
    listen 80;
    server_name procure.seudominio.com;   # <- altere aqui

    # Redireciona HTTP → HTTPS (ative após configurar SSL com certbot)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass         http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_cache_bypass $http_upgrade;
    }
}

# Bloco HTTPS — descomente após: certbot --nginx -d procure.seudominio.com
# server {
#     listen 443 ssl;
#     server_name procure.seudominio.com;
#
#     ssl_certificate     /etc/letsencrypt/live/procure.seudominio.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/procure.seudominio.com/privkey.pem;
#
#     location / {
#         proxy_pass         http://127.0.0.1:3000;
#         proxy_http_version 1.1;
#         proxy_set_header   Upgrade $http_upgrade;
#         proxy_set_header   Connection 'upgrade';
#         proxy_set_header   Host $host;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
